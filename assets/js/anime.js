$(function(){
    $(window).on('scroll',function (){
      let triggerClass = $('.gimi');
          animateClass = ('is-show');
   
      $(triggerClass).each(function(){
        let scroll       = $(window).scrollTop(),
            triggerTop   = $('.sc').offset().top,
            windowHeight = $(window).height();
        if (scroll > triggerTop - windowHeight/1.8){
          $(this).addClass("opc1");
        }
      });
    });
  });
  $(function(){
    $(window).on('scroll',function (){
      let triggerClass = $('.gimi0');
          animateClass = ('is-show');
   
      $(triggerClass).each(function(){
        let scroll       = $(window).scrollTop(),
            triggerTop   = $('.sc').offset().top,
            windowHeight = $(window).height();
        if (scroll > triggerTop - windowHeight/1.8){
          $(this).addClass("opc");
        }
      });
    });
  });
  $(function(){
    $(window).on('scroll',function (){
      let triggerClass = $('.gimi1');
          animateClass = ('is-show');
   
      $(triggerClass).each(function(){
        let scroll       = $(window).scrollTop(),
            triggerTop   = $('.sc').offset().top,
            windowHeight = $(window).height();
        if (scroll > triggerTop - windowHeight/1.8){
          $(this).addClass("opc0");
        }
      });
    });
  });
  $(function(){
    $(window).on('scroll',function (){
      let triggerClass = $('.gimi2');
          animateClass = ('is-show');
   
      $(triggerClass).each(function(){
        let scroll       = $(window).scrollTop(),
            triggerTop   = $('.sc').offset().top,
            windowHeight = $(window).height();
        if (scroll > triggerTop - windowHeight/1.8){
          $(this).addClass("opc2");
        }
      });
    });
  });
  $(function(){
    $(window).on('scroll',function (){
      let triggerClass = $('.gimibox');
          animateClass = ('is-show');
   
      $(triggerClass).each(function(){
        let scroll       = $(window).scrollTop(),
            triggerTop   = $('.sc').offset().top,
            windowHeight = $(window).height();
        if (scroll > triggerTop - windowHeight/1.8){
          $(this).addClass("opc");
        }
      });
    });
  });
  $(function(){
    $(window).on('scroll',function (){
      let triggerClass = $('.gimitex');
          animateClass = ('is-show');
   
      $(triggerClass).each(function(){
        let scroll       = $(window).scrollTop(),
            triggerTop   = $('.sc').offset().top,
            windowHeight = $(window).height();
        if (scroll > triggerTop - windowHeight/1.8){
          $(this).addClass("opc");
        }
      });
    });
  });
  $(function(){
    $(window).on('scroll',function (){
      let triggerClass = $('.test');
          animateClass = ('is-show');
   
      $(triggerClass).each(function(){
        let scroll       = $(window).scrollTop(),
            triggerTop   = $('.sc').offset().top,
            windowHeight = $(window).height();
        if (scroll > triggerTop - windowHeight/1.8){
          $(this).addClass("opc3");
        }
      });
    });
  });
  $(function(){
    $(window).on('scroll',function (){
      let triggerClass = $('.textl');
          animateClass = ('is-show');
   
      $(triggerClass).each(function(){
        let scroll       = $(window).scrollTop(),
            triggerTop   = $('.sc').offset().top,
            windowHeight = $(window).height();
        if (scroll > triggerTop - windowHeight/1.8){
          $(this).addClass("opc3");
        }
      });
    });
  });
  